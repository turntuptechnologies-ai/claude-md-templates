# {{ プロジェクト名 }}

{{ プロジェクトの概要。何を作るのか、何を解決するのかを簡潔に記述する。 }}

## プロジェクトフェーズ

現在: **{{ 現在のフェーズ }}**

| フェーズ | 意味 | 状態 |
|---|---|---|
| **PROTOTYPE** | 試作期 | 試作・検証中。コア機能の開発とプロトタイピング |
| **ALPHA / BETA** | 検証期 | 主要機能が揃い、フィードバック収集・改善中 |
| **PREVIEW** | 公開準備期 | 本番環境に近い状態で最終調整 |
| **STABLE** | 安定稼働期 | 正式リリース。安定稼働中 |

<!-- フェーズ名はプロジェクトに合わせてカスタマイズ可能。
     例: INCUBATION / HATCHING / EMERGING / CRUISING のようなテーマ性のある名前に変更してもよい。
     ユーザーから相談があれば、プロジェクトの世界観に合ったフェーズ名を一緒に考える。 -->

<!-- ▼ 関連プロジェクトがある場合はこのセクションを残す。単独プロジェクトなら削除する。 -->

## 関連プロジェクト

{{ 関連するリポジトリやサービスとの関係を記述する。 }}

<!-- 以下は記入例。 -->
- [関連プロジェクト名](URL) — 関係の説明（例: 本サービスから IoC データを取得しブロックリストとして利用する）

<!-- ▲ 関連プロジェクトセクションここまで -->

## 言語・ツール

{{ 使用する言語・フレームワーク・主要ライブラリを列挙する。 }}

- 言語: {{ 例: Rust, TypeScript, Python 等 }}
- フレームワーク: {{ 例: axum, React, Django 等 }}
- DB: {{ 例: PostgreSQL, SQLite 等（不要なら削除） }}
- ビルド: `{{ ビルドコマンド }}`
- テスト: `{{ テストコマンド }}`
- リント: `{{ リントコマンド }}`
- フォーマット: `{{ フォーマットコマンド }}`

<!-- ▼ 技術的な要件や仕様がある場合はこのセクションを残す。不要なら削除する。 -->

## 技術要件

{{ プロジェクト固有の技術要件・対応プロトコル・設定ファイル形式・ログ仕様等を記述する。 }}

<!-- 以下は記入例。プロジェクトに合わせて書き換えること。 -->

### 対応プロトコル

- {{ 例: UDP / TCP }}
- {{ 例: DoH (DNS over HTTPS) — RFC 8484 準拠 }}

### 設定ファイル

- {{ 例: TOML 形式（`config.toml`） }}
- {{ 例: サンプル: `config.example.toml` }}

### ログ

- {{ 例: RFC 3164 準拠の syslog 互換フォーマット }}

<!-- ▲ 技術要件セクションここまで -->

## アーキテクチャ概要

{{ プロジェクトのアーキテクチャを記述する。データフロー・主要コンポーネント・レイヤー構成等。
   プロジェクトの規模や性質に合わせて必要な項目を選択する。 }}

<!-- 以下は記入例。プロジェクトに合わせて書き換えること。 -->

### データフロー

1. {{ 例: IoC 収集 — 外部の脅威インテリジェンスソースから定期的に API へ POST }}
2. {{ 例: 蓄積 — PostgreSQL に IoC データを保存 }}
3. {{ 例: 配信 — 外部ユーザーが API 経由でデータを取得 }}

### 主要コンポーネント

- **{{ コンポーネント名 }}**: {{ 説明 }}
- **{{ コンポーネント名 }}**: {{ 説明 }}

<!-- ▼ CLI ツールの場合はこのセクションを残す。それ以外なら削除する。 -->

## コマンド体系

{{ CLI ツールのコマンド一覧を記述する。 }}

<!-- 以下は記入例。 -->

| コマンド | 説明 |
|----------|------|
| `{{ ツール名 }} {{ サブコマンド }}` | {{ 説明 }} |

<!-- ▲ コマンド体系セクションここまで -->

## ディレクトリ構成

{{ プロジェクトの主要なディレクトリ構成を記述する。
   全ファイルを列挙する必要はなく、初見で迷いやすい構成や重要なディレクトリの役割を説明する。 }}

<!-- 以下は記入例。 -->

```
src/
  main.ts              # エントリポイント
  api/                 # API ルートハンドラ
  config/              # 設定ファイル読み込み
  db/                  # データベースアクセス層
  services/            # ビジネスロジック
  middleware/          # ミドルウェア（認証・バリデーション等）
  validators/          # 入力バリデーション（Zod スキーマ等）
tests/
  integration/         # 統合テスト
docs/                  # ドキュメント
```

## コーディング規約

{{ プロジェクトで採用するコーディング規約を記述する。言語ごとに異なるため、プロジェクトに合わせて記入する。 }}

<!-- 以下は記入例。プロジェクトの言語に合わせて書き換えること。 -->

- コーディングスタイルガイド: {{ 参照するスタイルガイドがあれば記載 }}
- フォーマッタ: {{ 例: `cargo fmt`, `prettier`, `black` 等 }}
- リンタ: {{ 例: `cargo clippy`, `eslint`, `ruff` 等 }}
- 命名規則:
  - {{ 例: 型・クラス: PascalCase }}
  - {{ 例: 関数・変数: snake_case / camelCase }}
  - {{ 例: 定数: SCREAMING_SNAKE_CASE }}
  - {{ 例: ファイル名: kebab-case.ts / PascalCase.tsx }}

<!-- ▼ Rust プロジェクトの場合は以下を参考に unwrap/expect ルールを記述する。 -->

<!-- ### unwrap() / expect() ルール（Rust 向け）

- `unwrap()` / `expect()` はテストコードのみで使用し、本番コードでは `Result` / `Option` を適切に処理する
  - **検査対象**: `src/` 配下の本番コード（`#[cfg(test)]` ブロック、`tests/`、`benches/` は対象外）
  - **対象外（安全）**: `unwrap_or()`, `unwrap_or_default()`, `unwrap_or_else()` は適切なフォールバックがあるため対象外
  - **要修正（パニックリスク）**:
    - 外部入力のパース（ネットワークパケット、HTTP リクエスト、設定値）
    - ネットワーク応答・DB クエリ結果の処理
    - ファイル I/O の結果
    - チャネルの `send()` / `recv()`（相手側が切断される可能性）
  - **許容（コメント必須）**:
    - `Mutex::lock()` — poison されない前提
    - 正規表現リテラルの `Regex::new()` — コンパイル時に確定する文字列
    - 初期化時の致命的エラー（設定ファイル読み込み等）
    - 型変換で論理的に失敗しない箇所（直前の条件分岐で保証済み等）
  - 許容する箇所には `// unwrap safety:` コメントで理由を明記する -->

<!-- ▲ Rust unwrap ルールここまで -->

## テスト戦略

{{ テストの種類・命名規則・実行方法を記述する。 }}

<!-- 以下は記入例。プロジェクトに合わせて書き換えること。 -->

### テストの種類と実行方法

| 種類 | 対象 | 実行コマンド |
|------|------|-------------|
| 単体テスト | DB 不要のロジック | `{{ 例: deno test / cargo test }}` |
| 統合テスト | DB・外部サービスを含む | `{{ 例: deno task test:integration }}` |
| ベンチマーク | パフォーマンス計測 | `{{ 例: cargo bench（不要なら削除） }}` |

### テストファイルの配置

{{ テストファイルの命名規則と配置ルールを記述する。 }}

<!-- 以下は記入例（言語によって異なる）。 -->

<!-- TypeScript/Deno の場合:
- 単体テスト: 対象ファイルと同じディレクトリに `_test.ts` サフィックス（例: `auth.ts` → `auth_test.ts`）
- 統合テスト: 対象ファイルと同じディレクトリに `_integration_test.ts` サフィックス（例: `iocs.ts` → `iocs_integration_test.ts`）
-->

<!-- Rust の場合:
- 単体テスト: 同一ファイル内の `#[cfg(test)] mod tests` ブロック
- 統合テスト: `tests/` ディレクトリ
- ベンチマーク: `benches/` ディレクトリ
-->

## コミットメッセージ規約

[Conventional Commits](https://www.conventionalcommits.org/) に従う。

```
<type>: <description> (#<issue-number>)
```

### type 一覧

| type | 用途 |
|------|------|
| `feat` | 新機能の追加 |
| `fix` | バグ修正 |
| `docs` | ドキュメントのみの変更 |
| `test` | テストの追加・修正 |
| `refactor` | リファクタリング（機能変更なし） |
| `chore` | ビルド・CI・依存関係等の雑務 |
| `perf` | パフォーマンス改善 |

### ルール

- description は{{ 日本語 / English }}で記述する
- Issue 番号がある場合は末尾に `(#番号)` を付ける
- squash merge 時の PR タイトルもこの規約に従う

## 開発フロー

1. **Issue 作成** — コード・ドキュメント等の変更には必ず Issue を作成する
   - Issue のコメントに調査内容・試行錯誤・判断の経緯を記録する
   - 他の Issue を参照するときは番号だけでなく説明を付けた箇条書きにする（例: `- #42 — ユーザー認証機能`）
   - 適宜ラベルを付与する
   - `backlog` ラベル: 優先度が低く、通常の開発サイクルでは取り組まない Issue に付与する。「オープンの Issue に取り組んで」等の指示では `backlog` ラベルの Issue は対象外とする
2. **設計** — 設計チームが要件整理・アーキテクチャ設計を行い、Issue に設計内容を記載する
3. **設計レビュー** — レビューチームが設計の妥当性をチェックし、設計チームと相談・調整する
4. **ブランチ作成** — git worktree を使い、Issue に紐づくブランチで作業する
   - ブランチ命名規則: `issue-<番号>/<簡単な説明>` (例: `issue-42/add-user-auth`)
5. **実装** — 実装チームが設計に基づきコーディングする
   - 不明点は設計チーム・レビューチームに相談する
6. **PR 作成** — Issue を参照する (`Closes #42` 等)
7. **コミット・プッシュ** — 作業中は適宜 commit・push を行う（main ブランチへの直接 push は禁止）
8. **コードレビュー** — マージ前にレビューチームが PR をレビューする
9. **マージ** — squash merge を使用する
10. **ブランチ削除** — マージ後にブランチを自動削除する

## チーム解散前の必須チェック

チームを解散する前に、必ず以下の 3 つの検証を実施すること:

1. **設計との乖離チェック** — ドキュメント（CLAUDE.md, README.md 等）と実装コードに乖離がないか
2. **テストの不足チェック** — 新規・変更コードに対してテストが十分か、カバレッジに穴がないか
3. **ドキュメントの不足チェック** — 新機能・変更がドキュメントに反映されているか

発見された問題は Issue 化して対応してから解散する。

<!-- ▼ デプロイ先がある場合はこのセクションを残す。不要なら削除する。 -->

## デプロイ

{{ デプロイ先・デプロイ方法を記述する。 }}

<!-- 以下は記入例。 -->
- 当面: {{ 例: VPS（Docker / systemd） }}
- 将来: {{ 例: PaaS への移行を視野に入れる }}

<!-- ▲ デプロイセクションここまで -->

<!-- ▼ Dependabot を使う場合はこのセクションを残す。使わない場合は削除する。 -->

## Dependabot PR 対応方針

GitHub Dependabot が依存更新の PR を自動作成する。以下の基準で緊急度を判断し対応する。

| 緊急度 | 条件 | 対応 |
|--------|------|------|
| **即対応** | セキュリティ脆弱性修正（CVE あり、severity: high/critical） | 最優先でビルド・テスト確認後マージ |
| **早めに対応** | セキュリティ修正（severity: moderate/low）、メジャーバージョンアップ | 破壊的変更の有無を確認し、必要なら手動修正して対応 |
| **通常対応** | マイナー/パッチバージョン更新、dev 依存のみの更新 | 通常の開発サイクルで対応 |

### 対応手順

1. Dependabot PR の changelog・破壊的変更を確認する
2. ライセンスが permissive であることを確認する
3. 破壊的変更がある場合は Dependabot PR をベースにせず、新しいブランチで手動対応する
4. テスト・リント・フォーマットチェックの通過を確認する
5. マージ後、元の Dependabot PR が自動クローズされない場合は手動でクローズする

<!-- ▲ Dependabot セクションここまで -->

## ライセンスルール

{{ プロジェクトのライセンス方針を記述する。不要なら削除する。 }}

<!-- 以下は記入例。 -->
- GPL 系ライセンスの依存ライブラリは使用禁止（商用転用の可能性があるため）
- 許可: MIT, Apache-2.0, BSD-2-Clause, BSD-3-Clause, ISC, Zlib 等の permissive ライセンス
- 依存追加時はライセンスを必ず確認する

## 環境ルール

{{ プロジェクトの実行環境に関するルールを記述する。 }}

<!-- 以下は記入例。プロジェクトに合わせて書き換えること。 -->
- sudo は使用しない
- ツールやランタイムのインストールには mise を使用する
- サービスやミドルウェアが必要な場合は Docker を使用し、`docker-compose.yaml` で管理する

## 言語ルール

- Issue、コメント、PR の説明、コミットメッセージなど自然言語を書く箇所はすべて{{ 日本語 / English / 等 }}で記述する
